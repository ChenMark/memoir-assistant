# 2026-05-24 工作日志

## 忆往昔回忆录助手 — 技术提升修复

**项目路径**：`D:/浮点涌现/相关产品材料/忆往昔产品/忆往昔-(memoir-assistant) (4)/`

**修复输出目录**：`patched/`（共4个文件，不会覆盖原始文件）

### 完成内容

1. **安全加固 `patched/sdk.ts`**
   - 引入 `crypto-js`，用真实 MD5 替换假实现（原 `TelecomAuthService.md5()` 是 FNV-1a 变体）
   - 身份证号传输前 AES-256-CBC 加密（`PhoneAuthService.submitAuth`）
   - 移除全局顶层硬编码 `dashscope.api_key = 'your-dashscope-api-key'`
   - 新增 `OSSStorageService` 云存储服务层（前端不持有 AccessKey，通过后端 presigned URL 直传）
   - 新增工具函数 `md5()`, `hmacSha256()`, `aesEncrypt()`, `aesDecrypt()`

2. **安全加固 `patched/App.tsx`**
   - 身份证输入框改 `type="password"` 防肩窥
   - 添加手机号格式校验和身份证格式校验
   - 新增 `View.DRAFTS` → `<Drafts />` 路由
   - SDK 在组件外单例化（修复每次 render 重建问题）
   - 移除 `'https://your-backend-api.com'` fallback，改为警告

3. **新增 `patched/components/Drafts.tsx`**（草稿箱占位模块填充）
   - 草稿列表、阶段过滤（撰写中/待润色/已完成）
   - 预览全文、删除确认、导出 TXT
   - 导出 `saveDraftFromNarrative()` 和 `updateDraft()` 工具函数

4. **新增 `patched/.env.example`**（环境变量配置模板）

5. **新增 `patched/TECH_UPGRADE_REPORT.md`**（完整技术报告）

### 替换命令
```bash
cp patched/sdk.ts sdk.ts
cp patched/App.tsx App.tsx
cp patched/components/Drafts.tsx components/Drafts.tsx
cp patched/.env.example .env.local  # 然后填入真实值
```

---

## 开发环境调试验证（11:44-11:50）

### 问题修复

1. **dashscope 版本错误**：`package.json` 写 `dashscope@^1.20.0`，npm 最高版本仅 `1.0.1`。已修正为 `^1.0.1`。

2. **dashscope API 不兼容**：v1.0.1 的 API（chat/image/request）与 sdk.ts 期望的 `MultiModalversation.call()` + `baseConfig` 完全不匹配。解决方案：
   - 创建 `dashscope-polyfill.ts` — 基于 fetch() 直接调用 DashScope OpenAI-compatible API
   - 在 `vite.config.ts` 添加 alias：`'dashscope' → './dashscope-polyfill.ts'`

3. **缺失 index.css**：`index.html` 引用 `/index.css` 但文件不存在，已创建。

4. **缺失 node_modules**：已执行 `npm install`。

### 运行状态

- ✅ Vite dev server 启动在 `http://localhost:3000`
- ✅ 所有模块编译通过：index.tsx, App.tsx, sdk.ts, 7 个组件, types.ts
- ✅ crypto-js MD5/AES/HMAC 函数正常
- ✅ dashscope polyfill 正确 alias
- ✅ Drafts.tsx 补丁已应用
- ⚠️ `.env.local` 仍为 PLACEHOLDER 值，生产使用需填入真实 API Key

---

## 三大短板修复（12:00-12:10）

Martin大人提出「云存储缺失是最大短板，安全细节需要补强，UI占位模块填充」— 已全部修复。

### 1. 云存储真正接入（之前只定义了类但从未被使用）

**关键改动**:
- `App.tsx` → SDK 实例挂到 `window._memoirSDK` + `window._memoirBackendUrl`，所有组件可访问 `sdk.storage`
- `Drafts.tsx` → 保存时自动 OSS + localStorage 双写；加载时 OSS 优先 → localStorage 降级
- `Gallery.tsx` → `simulateCloudAuth()` 假功能彻底移除，替换为 `sdk.storage.upload()` 真实 OSS 上传
- `Friends.tsx` → 新增「云端备份」「云端恢复」两个按钮，数据存入 `memoir/backup/`
- `sdk.ts` → OSSStorageService 新增 `getDownloadUrl()` / `downloadObjectContent()` 方法；修复 `deleteObject` 使用 `config.deleteEndpoint`
- `server/src/index.ts` → 新增 `POST /oss/download` 端点（presigned GET URL）
- `.env.example` → 补全 `VITE_OSS_DELETE_ENDPOINT` / `VITE_OSS_STS_ENDPOINT`

### 2. 安全细节补强

- `sdk.ts` → `generateSign()` 升级为 HMAC-SHA256 优先（`VITE_TELECOM_SIGN_MODE=hmac`），MD5 降级兼容
- `sdk.ts` → 新增 `verifyRequestSignature()` 导出函数（供后端验证前端签名）
- `tsconfig.json` → 排除 `patched/`、`server/` 目录

### 3. UI 占位模块填充

- `Dashboard.tsx` → 硬编码假数据（`syncStatus: 85`、`totalWords: 12450` 等）全部替换为从 localStorage 实时读取；新增 `getStats()` 函数 + 5 秒轮询 + focus 自动刷新
- 云同步状态实时反映 `sdk.storage` 可用性

### 构建验证

`npx vite build` → ✅ built in 2.13s，1803 modules，0 errors

### bug 修复
- `sdk.ts` 第 547 行重复 `async uploadBatch(` 声明 → 已删除重复行
- `sdk.ts` 第 645-646 行重复 `private telecomAuthService` 声明 → 已删除重复行
- `Friends.tsx` 第 12-13 / 18-19 行重复 `STORAGE_KEY_*` 声明 → 已删除重复

---

## Web 版上架部署 — Vercel Serverless Functions 全栈（12:20-13:30）

### 项目重建
- 从零创建完整项目于 `C:\Users\MarkChen\WorkBuddy\2026-05-24-task-6\`
- React 18 + Vite 5 + TypeScript 5.9，40 个模块

### 后端重构为 Vercel Functions
- `api/oss/sign.ts` — OSS presigned PUT URL
- `api/oss/download.ts` — OSS presigned GET URL
- `api/oss/delete.ts` — OSS object delete
- `api/oss/list.ts` — OSS object list
- `api/telecom/token.ts` — 电信平台 token 代理
- `api/health.ts` — 健康检查端点

### 部署历程（共5次部署）
1. 初次部署 → TS7016 `ali-oss` 缺少类型声明
2. 添加 `@types/ali-oss` → TS2595 需用 default import
3. 修复 default import → TS2554 `list()` 缺第二个参数
4. 添加 `{}` options → **零警告构建** ✅
5. 最终部署 → ✅ 0 errors, 0 warnings

### 最终部署信息
- **主域名**: https://2026-05-24-task-6.vercel.app
- **平台**: Vercel Production
- **GitHub**: ChenMark (github.com)
- **vercel账号**: czmail-9969
- **构建时间**: ~15s, 40 modules
- **后端 API**: `/api/oss/*`, `/api/telecom/*`, `/api/health`
- **前端路由**: SPA fallback 配置完成

### 环境变量（需在 Vercel Dashboard 配置）
- `OSS_REGION`, `OSS_ACCESS_KEY_ID`, `OSS_ACCESS_KEY_SECRET`, `OSS_BUCKET`
- `TELECOM_APP_ID`, `TELECOM_APP_SECRET`, `TELECOM_REDIRECT_URI`
- 当前 OSS/TELECOM 为 `not_set`，需配置后启用云端功能

---

## GitHub 仓库推送（13:49）

- **仓库**: https://github.com/ChenMark/memoir-assistant
- **平台**: GitHub (ChenMark)
- **方式**: gh CLI + Personal Access Token
- **提交数**: 6 commits, master 分支
- **远程**: `origin https://github.com/ChenMark/memoir-assistant.git`

---

## GitHub Actions 自动部署配置（13:57）

- **Workflow 文件**: `.github/workflows/deploy.yml`
- **触发条件**: `push` to `master`
- **GitHub Secrets 已设**: `VERCEL_ORG_ID` (team_i8M4gaaDAbCsxLUKyDOSNOQ9), `VERCEL_PROJECT_ID` (prj_egznFf7rIHXjqyY7VvInn4samR1Q)
- **缺失**: `VERCEL_TOKEN` — 需要在 https://vercel.com/account/tokens 手动创建 classic token
- **Token 权限**: 新 GitHub token 含 `workflow` 权限，可运行 Actions
- **Token 权限详情**: `admin:public_key`, `project`, `repo`, `workflow`, `write:discussion`, `write:packages`（仅缺 `read:org`）
- **git remote 已嵌入 token**，无需每次手动认证

---

## Vercel Preview Deployments 配置（16:47）

- **Preview Workflow**: `.github/workflows/preview.yml`
- **触发条件**: `pull_request` to `master`
- **效果**: 每个 PR 自动生成独立预览链接（临时域名），评论到 PR 中
- **双 workflow 并行**: `deploy.yml`（生产） + `preview.yml`（预览）互不冲突

---

## 冒烟测试机器人（17:04）

- **集成到**: `preview.yml` → `smoke-test` job
- **触发**: PR 预览部署完成后自动执行
- **测试范围**: `/api/health`、首页、`/api/oss/sign`、`/api/oss/download`、`/api/oss/list`、`/api/telecom/token`
- **输出**: 测试报告表格自动评论到 PR
